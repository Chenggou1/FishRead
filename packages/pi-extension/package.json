{
  "name": "@fishread/pi-extension",
  "version": "0.1.1",
  "type": "module",
  "description": "FishRead Pi extension — read EPUB in the conversation thread",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fishread/sdk": "workspace:*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "latest",
    "@earendil-works/pi-tui": "latest",
    "typescript": "^5.0.0"
  }
}
